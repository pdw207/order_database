= form_for @supplier do |f|
	- if @supplier.errors.any?
		#error_explanation
		
		%h2
			= pluralize(@supplier.errors.count, "error")
			prohibited this supplier from being saved:
		%ul
			- @supplier.errors.full_messages.each do |msg|
				%li= msg
	%p.center
		= f.label :tipo, "Tipo", style:"text-align:center;"
		= f.select(:tipo, options_for_select([ "Materiales","Herramientas", "Servicios", "Otros" ], @supplier.tipo))
	%p.center
		= f.label :name, "Nombre de Empresa", style:"text-align:center;"
		= f.text_field :name
	%p.center
		= f.label :name, "Nombre de Contacto", style:"text-align:center;"
		= f.text_field :contact_name
	%p.center
		= f.label :name, "Teléfono", style:"text-align:center;"
		= f.text_field :phone
	%p.center
		= f.label :name, "Email", style:"text-align:center;"
		= f.text_field :email
	%p.center
		= f.label :name, "Dirección", style:"text-align:center;"
		= f.text_field :address1
		= f.text_field :address2
		= f.text_field :address3

	%p.center
		= f.label :name, "Notas", style:"text-align:center;"
		= f.text_area :notes, :cols => "40", :rows => "3"

	%footer= f.submit "Enviar", class: "btn btn-default", :as => :input

